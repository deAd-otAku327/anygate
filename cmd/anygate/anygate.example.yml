# example config
routes:
  /api/v1/basic: http://localhost:8080/health
  /socket.io/ : http://localhost:8000/socket.io/

proxy:
  timeout: 30s

static:
  compress: true                              
  skip_cache: true                           

swagger:
  - name: service-1
    url: https://petstore.swagger.io/v2/swagger

  - name: service-2
    url: https://httpbin.org/spec.json
